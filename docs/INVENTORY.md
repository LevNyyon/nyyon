# nyyon-app inventory — CHECK WHAT YOU WANT SAVED

**Checked `[x]` = SAVE in the new app. Unchecked `[ ]` = REMOVE.**
I pre-checked my recommendations — flip anything and hand it back.

## Tools (246)

### Core · activity bus — `activity-log.js` (2)

- [x] `recent_events` — Append-only activity log, newest first.…
- [x] `log_note` — Append a free-form note into the activity log.…

### AEO engine · taste learning — `aeo-feedback-editing.js` (2)

- [x] `aeo_react` — Record the operator's reaction to an article/question IDEA so Nyo learns their editorial taste over time.…
- [x] `aeo_edit_question` — Edit a saved AEO question in place — title, target keyword, priority, angle/notes, or voice.…

### AEO engine (headless, feeds the Blog writer) — `aeo-interview-write.js` (4)

- [x] `aeo_add_question` — Add a brand-new AEO article topic to the queue as a pending question.…
- [x] `aeo_start_interview` — Start an expert interview for an AEO article before writing it.…
- [x] `aeo_write_with_answers` — Submit the operator's interview answers and WRITE the AEO article from their expertise — it then publishes and auto-deploys live to the public site.…
- [x] `aeo_write_now` — Write + publish a queued AEO question that is already READY (expert context baked in — from the Sunday Brain, or a completed interview).…

### AEO engine · suggestions — `aeo-suggestions.js` (4)

- [] `generate_aeo_suggestions` — Scan the top-scored, unconverted OSINT signals and develop up to N of them into AEO article suggestions (title + the company's angle + rationale), pending operator approval.…
- [] `list_aeo_suggestions` — List AEO article suggestions (OSINT-sourced topics awaiting your decision).…
- [] `approve_aeo_suggestion` — Approve an AEO suggestion: creates the aeo_questions row with the developed angle pre-filled as the expert interview (skips the Q&A), then writes the FULL article (voice + playbook…
- [] `reject_aeo_suggestion` — Reject an AEO suggestion — dismisses it and its source signal so it is never re-suggested.

### Blog — `blog-posts.js` (12)

- [x] `list_blog_posts` — List blog post stubs (slug + title + excerpt + published date).…
- [x] `read_blog_post` — Read one blog post by slug.…
- [x] `write_blog_post` — Write a blog post the RIGHT way.…
- [x] `edit_blog_post` — Safely edit an EXISTING blog post.…
- [x] `regenerate_blog_image` — Generate or regenerate the featured image for a published blog post.…
- [x] `regenerate_blog_cover` — Regenerate a blog post's featured cover the RELIABLE way — the SAME code-drawn cover the article-creation pipeline uses (brand SVG → PNG, no AI image model, no external API key, no…
- [x] `generate_social_card` — Generate a social-media card (LinkedIn/Instagram graphic) for a blog post or a custom message.…
- [x] `publish_blog_post` — Publish a blog post LIVE to the public site.…
- [x] `reshape_blog_post` — Fix an EXISTING post that was saved as raw markdown or looks unformatted / has no visuals: rewrites its body into clean house-style HTML (brand voice + learned taste, banned phrase…
- [x] `expand_blog_post` — Expand + upgrade an EXISTING post: deepen the story (history, why-now, failure modes, concrete examples) to ~1,600-2,200 words, weave in the company's upside substantively (not a pitch),…
- [x] `article_from_social_post` — Expand a social post into a full blog ARTICLE draft.…
- [x] `delete_blog_post` — Delete a blog post from the Command Center by slug (a draft, duplicate, or test post).…

### Calendar store (page cut, Blog/Social/Hot Takes mirror into it) — `calendar.js` (4)

- [x] `list_calendar_events` — List calendar events in a time range.…
- [x] `read_calendar_event` — Read one calendar event by id (ce_…).
- [x] `write_calendar_event` — Create or update a calendar event.…
- [x] `delete_calendar_event` — Remove a calendar event by id.…

### Nyo chat — `conversations.js` (4)

- [x] `list_conversations` — List past Nyo conversations, newest first: id, title (derived from the first thing the operator said), message count and timestamps.…
- [x] `read_conversation` — Read one past conversation in full: every user and assistant turn in order, with the tools that ran.…
- [x] `rename_conversation` — Give a past conversation an explicit title so it is easy to find later.…
- [x] `delete_conversation` — Permanently delete one PAST conversation and all of its messages.…

### Daily Planner — `daily-planner.js` (7)

- [x] `read_daily_plan` — Read a day's plan (schedule + to-dos).…
- [x] `save_daily_plan` — Write (or replace) the day's plan — this is what fills the plan panel.…
- [x] `update_daily_plan` — Edit part of a day's plan during the day (mark a block/to-do done, add a to-do, adjust the schedule).…
- [x] `search_daily_plans` — Search past daily plans by text (matches summary, to-dos, blocks).…
- [x] `list_recent_plans` — The most recent PAST daily plans (default last 3 days, before today).…
- [x] `read_weekly_objectives` — Read the current week's strategic objectives (or a given week_start, YYYY-MM-DD Sunday).…
- [x] `set_weekly_objectives` — Set this week's strategic objectives — the 2-4 outcomes that move the business in the intended direction.…

### Blog · site rebuild — `deploy-public-site.js` (1)

- [] `deploy_public_site` — FULL-SITE rebuild + deploy of the public site (homepage, templates, static assets) via the LOCAL deploy sidecar on the operator's Mac.…

### Core · settings — `feature-flags.js` (2)

- [x] `list_feature_flags` — List every feature flag with current value + scope.…
- [x] `set_feature_flag` — Flip a feature flag on or off.…

### Prospecting (GTM engine) — `gtm.js` (28)

- [] `gtm_wa_candidates` — List WhatsApp people as GTM intake candidates — DM chats and group senders (contacts AND non-contacts), or one group's live roster with group_id.…
- [] `gtm_import_from_wa` — Add selected WhatsApp people to GTM intake as leads — pass people:[{phone, name}] (from gtm_wa_candidates).…
- [] `gtm_import_leads` — GTM intake: import a phone list (pasted text — one number per line or CSV with a phone column — or a URL to fetch).…
- [] `gtm_list_leads` — List GTM leads.…
- [] `gtm_read_lead` — Read one GTM lead in full: fields + provenance (sources/conflicts) + org chart people + outreach angles.
- [] `gtm_update_lead` — Manually edit a GTM lead's fields: identity (name, linkedin — the PERSON's profile, email, company, position), plus company_linkedin (the COMPANY's linkedin.com/company/ page) and …
- [] `gtm_enrich_lead` — Run the full enrichment chain on one GTM lead (accuracy order: WhatsApp identity → company-from-LinkedIn → PDL (paid, auto-skipped when name+company present) → Twilio line-type → G…
- [] `gtm_enrich_batch` — Drain the enrichment queue for a whole intake batch — a handful of leads per call, returning {enriched, remaining}.…
- [] `gtm_api_usage` — Usage meters for the paid enrichment APIs: PDL (used/limit + remaining credits from its own headers), SerpApi (live account numbers), Twilio (account balance).…
- [x] `gtm_api_limits_update` — Update the GTM API limits config (the gtm-api-limits knowledge doc): per provider monthly_limit / renewal_day (day of month the plan resets, 1-28) / warn_at_pct, and twilio.balance…
- [x] `gtm_check_whatsapp` — Look a phone number up on WhatsApp via the shared wa-gateway: registered?, pushname, profile photo, about text, business flag.…
- [x] `gtm_org_chart` — Fetch (or read the cached) org chart for a GTM lead's company from theorg.com — real names, titles, hierarchy.…
- [x] `gtm_score_icp` — Score a GTM lead against the ICP (the editable `brand-icp` knowledge doc): strong/medium/weak + reason/gap tags.…
- [x] `gtm_company_context` — Gather everything about the COMPANY behind a GTM lead in one pass: theorg org chart, LinkedIn headcount (staff_count — the ICP's size band), and LinkedIn open roles.…
- [x] `gtm_open_roles` — Fetch a GTM lead company's open roles from LinkedIn (company id resolved once via the throttled linkedin-gateway, cached; jobs via the public guest API from the gateway's residenti…
- [x] `gtm_outreach_angles` — Generate ranked outreach angles + draft WhatsApp bubbles for a GREEN GTM lead (one Opus call driven by the gtm-you / gtm-outreach-playbook / -rules / -examples knowledge docs + the…
- [x] `gtm_save_angles` — Persist an edited outreach angles payload for a GTM lead (whole payload replace — read it first with gtm_read_lead, edit the bubbles, save it back).
- [x] `gtm_send_outreach` — SEND outreach bubbles to a GTM lead's WhatsApp through the shared gateway, humanly paced (4-9s jittered gaps, stops on first failure, max 4 bubbles, all logged).…
- [x] `gtm_lead_thread` — A GTM lead's WhatsApp conversation, honestly sourced: engine sends (sent/failed), manual outbound from the operator's own WhatsApp, and the lead's replies — each message with dir i…
- [x] `gtm_schedule_send` — SCHEDULE outreach bubbles to a GTM lead's WhatsApp for a future time (ms epoch).…
- [x] `gtm_cancel_scheduled` — Cancel a scheduled send before it fires, or dismiss a terminal failed/partial one (a claimed/in-flight send cannot be un-fired).
- [x] `gtm_list_scheduled` — List scheduled sends (default: live + failed ones).…
- [x] `gtm_run_due_scheduled` — Run the scheduled-send tick NOW (same pass the cron runs): claim due schedules atomically and send them.…
- [x] `gtm_lead_to_pipeline` — Promote a GTM lead into the Pipeline CRM: creates a linked contact + client row at stage 'target' (idempotent — re-running returns the existing client_id).
- [x] `gtm_enrich_sources` — Run ONE raw enrichment source directly (no lead required): pdl {phone,name,region,country}, twilio {phone}, or serp {q}.…
- [x] `gtm_green_leads` — List the GREEN (fully identified) GTM leads ready for context enrichment + outreach.…
- [x] `gtm_identity_audit` — Audit every GTM lead whose assigned LinkedIn profile does NOT match their name (the wrong-person / namesake bug).…
- [x] `gtm_you` — Read the GTM operator profile (gtm-you doc: name/role/business/location + WhatsApp groups + warm connections — drives outreach positioning and warm-path matching).…

### Hot Takes feed engine · signals/sources — `heartbeat.js` (9)

- [x] `list_heartbeat_sources` — List the industry-awareness feed sources (OSINT module 'Sources' tab): RSS feeds + Google News topic queries the hourly heartbeat ingests.…
- [x] `write_heartbeat_source` — Add or edit an industry-awareness feed source.…
- [x] `delete_heartbeat_source` — Delete an industry-awareness feed source by id (its already-ingested signals are kept).…
- [x] `industry_pulse` — Get the company's current industry awareness — what's happening in the industry right now and what the company could do about it.…
- [x] `hot_topics` — The company's synthesized OSINT HOT TOPICS — sharp, blog-grade angles on what is happening right now, clustered from real industry signals.…
- [x] `list_signals` — List recent scored industry signals (real news/blog items from the heartbeat).…
- [x] `signal_to_blog` — Turn an industry signal into a blog opportunity — creates an AEO question seeded with the signal's angle (priority 2, timely).…
- [x] `read_signal` — Read the FULL article behind a heartbeat signal (not just its title) and return the text so you can react to what it actually says.…
- [x] `signal_to_social` — Draft a LinkedIn/social post reacting to an industry signal, in the company voice.…

### Hot Takes — `hot-takes.js` (19)

- [x] `hottake_list_packages` — List Hot Takes publication packages (the editorial pipeline).…
- [x] `hottake_read_package` — Read one Hot Takes package by id — its full topic/take/brief/article/review state plus its social posts and the single next action the operator should take.
- [x] `hottake_topics_of_the_day` — The prioritized live feed of topics worth a company response — the synthesized hot topics + scored industry signals + the actionable digest, deduped and ranked.…
- [x] `hottake_add_topic` — Manually add a topic to Selected Topics (creates a publication package at status "topic").…
- [x] `hottake_add_link` — Turn a pasted article URL into a standard topic card — fetches the page, extracts title/source/summary/date with the LLM, and adds it to Selected Topics.…
- [x] `hottake_pin_topic` — Pin a Topic-of-the-Day card into Selected Topics as a publication package.…
- [x] `hottake_dismiss_topic` — Dismiss a Hot Takes package (mark it not relevant).…
- [x] `hottake_patch_package` — Edit fields on a Hot Takes package (title, summary, take + its four inputs, headline, notes, status, pinned).…
- [x] `hottake_draft_take` — Propose the company's take on a topic: a specific, defensible point of view (never a neutral summary) plus the four take inputs — what the company believes, what is commonly misund…
- [x] `hottake_build_brief` — Build the short editorial brief from the approved take: argument, audience, why-now, 3-5 supporting points, evidence available, likely objections, recommended conclusion, and the p…
- [x] `hottake_write_article` — Write the full article (1,500+ words) from the approved brief through the shared house-style writer — lands as a blog draft (with figures + cover) linked to the package, status mov…
- [x] `hottake_review_scan` — Scan the written article for review: extract the important factual claims (typed directly_supported / company_experience / opinion / unsupported, with needs-confirmation status) an…
- [x] `hottake_draft_social` — Draft the LinkedIn distribution posts for a publication — the company post (the organization's position) and the personal post (direct, experiential, first-person; NOT a copy).…
- [x] `hottake_schedule_release` — Schedule a publication in one action: the website publish plus both LinkedIn posts.…
- [x] `hottake_cancel_schedule` — Cancel a scheduled publication: the package returns to ready (unscheduled), queued LinkedIn legs go back to ready/draft, and the calendar entry is marked cancelled.…
- [x] `hottake_publish_website` — Publish the package's article to the public site NOW through the shared blog pipeline (edge-rendered, live in ~60s).…
- [x] `hottake_post_leg` — Send one social leg (a hot_take_posts row) now through the shared social gateway, logged to the outbox.…
- [x] `hottake_patch_post` — Edit one social leg: the final post text (body), notes, image_url, scheduled_at, or status (draft/ready/scheduled/skipped/not_planned — use not_planned to mark a channel intentiona…
- [x] `hottake_timing_defaults` — Read the recommended release timing (default publish hour + per-leg offsets) from the editable hottakes-timing note.

### Knowledge — `knowledge.js` (5)

- [x] `list_knowledge` — List every knowledge doc (slug + title + scope).…
- [x] `read_knowledge` — Read one knowledge doc by slug.…
- [x] `read_knowledge_path` — Return the breadcrumb chain root → … → :slug for a knowledge doc.…
- [x] `write_knowledge` — Create or update a knowledge doc.…
- [x] `delete_knowledge` — Delete a knowledge doc.…

### Core · KPI config + outreach stats — `kpi.js` (6)

- [] `outreach_signature` — Read or edit the rubric the KPI uses to tell a genuine outreach message from a follow-up or personal chat (the outreach-signature knowledge doc).…
- [] `outreach_sentiment_rubric` — Read or edit the rubric that scores how a prospect FEELS in their reply (positive/neutral/negative) for the Digest KPI drawer (the outreach-sentiment knowledge doc).…
- [] `kpi_reclassify` — Refresh the KPI content data now: classify not-yet-judged outbound WhatsApp messages AND sync + classify LinkedIn messages you sent (from the ~24h window).…
- [] `kpi_outreach_log` — Today's itemized outreach attempts behind the KPI — every LinkedIn touch (with the exact copy sent) and outbound WhatsApp message, each with who, when, channel, and the message bod…
- [] `kpi_outreach_status` — Today's outreach KPI vs the daily goal (default 20/work day) across LinkedIn + WhatsApp: done, remaining, pct, per-channel split, and pace (status: off_day/behind/on_track/done, pl…
- [] `kpi_outreach_settings` — Read or change the outreach KPI rules (the kpi-outreach knowledge doc): daily_target (NEW outreaches per work day), tz, work_days (0=Sun..6=Sat), work_hours [start,end].…

### Calendar store · reminders — `meeting-reminders.js` (2)

- [] `get_meeting_reminders` — Read the WhatsApp meeting reminder setup: enabled/disabled, lead minutes before the meeting, target chat, plus upcoming meetings and whether each will fire a reminder.…
- [] `set_meeting_reminders` — Configure WhatsApp meeting reminders: turn them on/off, change how many minutes before the meeting the reminder fires, change the target WhatsApp chat id, or which calendar kinds t…

### Hot Takes feed engine · listeners (Sources tab) — `osint.js` (8)

- [x] `list_osint_listeners` — List the scraper engines (hn / reddit / stackoverflow / github / appstore / website / duckduckgo) with their enabled state, cadence, last-run timestamp, and lifetime totals.…
- [x] `toggle_osint_listener` — Enable, disable, or re-cadence an OSINT scraper engine.…
- [x] `list_osint_targets` — List the brands / companies we monitor.…
- [x] `read_osint_target` — Read one OSINT target by id.
- [x] `write_osint_target` — Create or update an OSINT target.…
- [x] `delete_osint_target` — Remove an OSINT target and its mentions.…
- [x] `scrape_osint_target` — Run the enabled OSINT listeners against one target.…
- [x] `list_osint_mentions` — List recent mentions / conversations harvested by OSINT listeners.…

### Outreach · cohorts/queue — `outreach-cohorts.js` (17)

- [x] `outreach_cohort_list` — The outreach queue: every prospect we decided to approach, with which named queue they are in, what we last said to them, what the next scheduled message is and when it goes, and w…
- [x] `outreach_cohorts` — List the named outreach queues (campaigns) with how many prospects each holds — total, active, and answered.…
- [x] `outreach_cohort_create` — Create a named outreach queue.…
- [x] `outreach_cohort_update` — Change a cohort settings — name, status (active | paused | finished | canceled), timezone, start_hour/end_hour, send_days (0=Sunday…6=Saturday) and languages.…
- [x] `outreach_step_draft` — Draft the copy for ONE step of a cohort sequence, using the cohort name and a sample of who is actually in it as the brief.…
- [x] `outreach_cohort_delete` — Delete an empty named queue.…
- [x] `outreach_sequence` — Read or write a QUEUE's message sequence — the copy the whole cohort receives, written once and personalised per recipient.…
- [x] `outreach_cohort_go_live` — Schedule specific STAGED prospects to start receiving their cohort's sequence.…
- [x] `outreach_cohort_add_many` — Add several prospects to one named queue at once (the Prospecting → Qualification bulk action).…
- [x] `outreach_cohort_enroll` — Put ONE prospect on a named outreach queue by lead_id.…
- [x] `outreach_cohort_approve` — Approve (or un-approve) the NEXT message for one or more prospects in a cohort.…
- [x] `outreach_cohort_edit_message` — Rewrite the next message for ONE prospect, for that prospect only — the cohort's own copy is untouched and everyone else still gets what was authored for the group.…
- [x] `outreach_cohort_reschedule` — Move WHEN one prospect's next message goes out.…
- [x] `outreach_cohort_control` — Pause, resume, stop, or unschedule the automated ladder for one queued prospect.…
- [x] `outreach_cohort_remove` — Take a prospect off the outreach queue entirely (deletes the enrolment row).…
- [x] `outreach_cohort_tick` — Run the outreach queue once: for every prospect whose next message is due, re-read the conversation, drop anyone who has replied, and send the next approved bubble.…
- [x] `outreach_cohort_settings` — Read or change the outreach queue cadence (the outreach-cohort-cadence knowledge doc): step_delays_hours (gap before each follow-up), max_sends_per_day, min_gap_minutes, quiet_star…

### Outreach · replies to pipeline — `outreach-promote.js` (3)

- [x] `outreach_collect_replies` — Collect everyone who REPLIED to our outreach — LinkedIn (li_prospects marked replied) + GTM WhatsApp (an inbound message after our first send) — normalized to {source,name,company,…
- [x] `outreach_promote_replies` — Put replied people on the sales pipeline: create a new prospect client at the replied stage, or advance an existing one forward (never backward).…
- [x] `outreach_promotion_settings` — Read or change how replies become deals (the outreach-promotion knowledge doc): replied_stage (where a reply lands), advance_only (never move a further-along deal back), stage_rank…

### Outreach · conversations — `outreach-wa.js` (5)

- [x] `outreach_wa_threads` — List the WhatsApp conversations that belong to PROSPECTS (people in the GTM lead store we have a 1:1 chat with) — the Outreach module's Conversations tab.…
- [x] `outreach_mark_dead` — Mark a prospect's conversation dead so it drops out of the working list, or bring it back with dead:false.…
- [x] `outreach_wa_thread` — Open one prospect conversation: the full WhatsApp message history (oldest first) plus the prospect context card — name, company, title, ICP fit and why, LinkedIn, open roles, org s…
- [x] `outreach_draft_reply` — Suggest the next WhatsApp message to a prospect.…
- [x] `outreach_wa_settings` — Read or change how the Outreach WA tab behaves (the outreach-reply-drafting knowledge doc): the drafting rules text, and the limits thread_limit / message_limit / draft_context_mes…

### Social — `social-gateway.js` (2)

- [x] `list_social_integrations` — List the social CONNECTIONS Nyo can post through, and whether each is configured.…
- [x] `post_to_social` — Publish to a social profile through the social gateway.…

### Social — `social-posts.js` (6)

- [x] `create_social_post` — Add a NEW standalone social post to the Social module's review queue — a post the operator wrote WITH you (in chat), NOT derived from a blog article.…
- [x] `draft_social_posts` — DRAFT social posts for a published blog article — one per channel (linkedin-company, facebook-company, linkedin-personal), each in the right voice, into the review queue as status …
- [x] `list_social_posts` — List social posts drafted from published blog articles.…
- [x] `read_social_post` — Read one social post by id — returns its channel, status, source article (blog_title/blog_slug), and full current content.
- [x] `edit_social_post` — Replace a social post's content with a new version.…
- [x] `delete_social_post` — Delete a social post from the Social module by id (a draft you no longer want, a duplicate, or a test post).…

### AEO engine · Sunday Brain — `sunday-brain.js` (2)

- [] `brain_start` — Start (or resume) this week's Sunday Brain — the weekly editorial interview.…
- [] `brain_submit_answers` — Submit the operator's answers to this week's Sunday Brain and DERIVE + SCHEDULE the coming week's article slate.…

### Core · web fetch — `web.js` (1)

- [x] `web_fetch` — Fetch a public web page or JSON/text endpoint over http(s) and return its readable text (HTML stripped, prefers the article/main body, capped).…

### WhatsApp shared (Outreach/Prospecting/Nyo) — `whatsapp.js` (15)

- [x] `wa_backfill_lid_map` — Resolve outbound WhatsApp @lid (privacy-id) chats to their real phone numbers and cache them (wa_lid_map), so lead↔chat matching can tell you've reached out to someone messaged on …
- [x] `send_whatsapp` — Send a WhatsApp text message via the wa-gateway.…
- [x] `send_whatsapp_image` — Send an image to a WhatsApp chat.…
- [x] `send_whatsapp_document` — Send a document (PDF etc.) to a WhatsApp chat.…
- [x] `react_whatsapp` — React to a WhatsApp message with an emoji.
- [x] `list_wa_groups` — List every WhatsApp group on the connected session.…
- [x] `list_wa_chats` — List WhatsApp chats from the local DB (groups AND 1:1 DMs), optionally filtered.…
- [x] `find_wa_chat` — Find a WhatsApp chat/person by partial name or phone — use this (NOT list_wa_chats) whenever the operator refers to a WhatsApp contact by name, e.g.…
- [x] `set_wa_chat_listening` — Turn the digest's listener ON or OFF for one WhatsApp chat.…
- [x] `read_wa_group_participants` — Get the full participant roster for a WhatsApp group — phone numbers, display names where known, admin flags.…
- [x] `read_registry` — The LIVE system registry — every external gateway (with configured/missing status), your real tools grouped by domain (derived from the running tool registry, not a hand-list), the…
- [x] `system_health` — Probe every service Nyo depends on and report status: the app worker itself, the WA gateway + session (linked/ready), and the GTM gateways (theorg org charts reachability + which…
- [x] `restart_wa_session` — Bounce the WhatsApp puppet — stop the session, restart it, poll until status=ready.…
- [x] `backfill_wa_messages` — Pull recent messages wa-gateway has cached and insert them into the local wa_messages table so the digest + funnel have data to mine.…
- [x] `toggle_wa_chat_auto_listen` — Flip the auto_listen flag on a WhatsApp chat (group or DM).…

### Core · workflow runner — `workflows.js` (6)

- [x] `list_workflows` — List every workflow defined in the system.…
- [x] `read_workflow` — Read one workflow by slug.…
- [x] `write_workflow` — Create or update a RUNNABLE workflow: an ordered list of EXISTING tools with no logic of its own (rules live in knowledge; branching belongs in a tool).…
- [x] `run_workflow` — EXECUTE a stored workflow through the generic runner: each step dispatches into the shared tool pool with a threaded context; the full trail lands in workflow_runs + workflow_step_…
- [x] `delete_workflow` — Delete a workflow.…
- [x] `list_workflow_runs` — List recent workflow runs (every fire of a workflow leaves a row here).…

### CRM clients (cut) — `clients.js` (5)

- [ ] `list_clients` — List clients — the companies the operator works or worked for.…
- [ ] `read_client` — Read one client by id, including every contact linked to it.…
- [ ] `write_client` — Create or update a client (company / account).…
- [ ] `delete_client` — Erase / permanently delete a client — a company / prospect / account — from the pipeline and CRM by id.…
- [ ] `query_crm` — Run a READ-ONLY SQL query (SELECT/WITH only) to get EXACT counts, group-bys, and filters across the CRM.…

### CRM contacts (cut) — `contacts.js` (5)

- [ ] `list_contacts` — List contacts.…
- [ ] `contact_taxonomy` — Return enum lists (statuses, sources) plus distinct owners and tags currently in use.…
- [ ] `read_contact` — Read one contact by id.…
- [ ] `write_contact` — Create or update a contact.…
- [ ] `delete_contact` — Erase / permanently delete a person (contact) from the CRM by id.…

### Digest (page cut, Hot Takes reads its items) — `digest.js` (9)

- [ ] `digest_learn_interests` — Learn from the digest items you've been DISMISSING and update the interest filter (the nyyon-digest-interests knowledge doc) so those kinds of items stop being surfaced.…
- [ ] `list_digest` — Read today's digest items — the actionable morning brief.…
- [ ] `digest_stats` — Counts: total / unread / high-urgency / actionable / starred.…
- [ ] `generate_digest` — Scan recent activity and materialize new digest items.…
- [ ] `add_digest_item` — Manually drop something into the digest.…
- [ ] `mark_digest_read` — Mark a digest item as read (dismisses it from the unread list).…
- [ ] `star_digest_item` — Pin a digest item by starring it.…

### Finance (cut) — `finance.js` (4)

- [ ] `list_finance` — Read the monthly cashflow calculator (the 'Agency Calculator').…
- [ ] `add_finance_entry` — Add a row to the cashflow calculator.…
- [ ] `update_finance_entry` — Update a cashflow row by id (from list_finance).…
- [ ] `delete_finance_entry` — Delete a cashflow row by id (from list_finance).

### Funnel (cut) — `funnel.js` (11)

- [ ] `funnel_overview` — Dashboard snapshot for the marketing funnel.…
- [ ] `funnel_stats` — Lower-level funnel counts over a rolling window — visits, sessions, preq submits, leads, mqls, booked, disqualified, plus stage-to-stage rates and event-type breakdown.…
- [ ] `list_sessions` — Recent web sessions — UTM attribution, click IDs, landing path, device, person email.…
- [ ] `list_events` — Recent web events (visit, scroll_depth, cta_click, preq_submit, newsletter_signup, etc).…
- [ ] `list_web_identities` — Identified people (email-keyed).…
- [ ] `read_web_identity` — Full drill-down for one identity — linked cookies, identity_links audit, recent sessions, recent events, conversions.…
- [ ] `list_conversions` — Recent conversion records — every funnel-stage transition (lead/mql/sql/negotiations/deal/contract/client/dropped/disqualified) with person + value.
- [ ] `list_pipeline_stages` — List the 9 pipeline stages with channel + tone.…
- [ ] `people_at_stage` — Everyone who reached a specific pipeline stage in a window.…
- [ ] `log_conversion` — Mark a person as having reached a pipeline stage.…
- [ ] `identify_by_email` — Stitch a cookie to an email.…

### LI Outreach (cut) — `li-outreach.js` (17)

- [ ] `li_outreach_intake` — Fetch prospect candidates from a pasted LinkedIn people-search URL (or plain keywords).…
- [ ] `li_outreach_add` — Enroll candidates (from li_outreach_intake) into the outreach pool.…
- [ ] `li_outreach_list` — List LI Outreach prospects with their sequence position (step_index, next_action_at, status: active/paused/done/stalled), plus the global pause flag and today's send counts.…
- [ ] `li_outreach_prospect` — One prospect's full picture: the row, every touch already sent (with the exact copy + sent/failed), and the rendered per-step plan for THIS person.…
- [ ] `li_outreach_control` — Play/pause the outreach.…
- [ ] `li_outreach_remove` — Remove a prospect from the pool entirely (their touch history goes too).…
- [ ] `li_outreach_sequence` — Read or replace the outreach sequence (the li-outreach-sequence knowledge doc): connect+note → wait Nd → message → wait Nd → message.…
- [ ] `li_outreach_voice` — Read or replace the LI Outreach message VOICE — the editable rules the writer follows for every connection note and DM (tone, length, what to avoid, personalization + never-invent …
- [ ] `li_outreach_settings` — Read or patch the throttling rules (the li-outreach-throttle knowledge doc): max_connections_per_day, max_messages_per_day, per_tick_cap, jitter_max_seconds, active_hours_utc [star…
- [ ] `li_outreach_prospect_sequence` — Customize the outreach sequence for ONE lead, or reset it to the global sequence.…
- [ ] `li_outreach_resolve` — Generate the ACTUAL messages for a lead and lock them onto that lead's sequence, so the exact words (not just the intent) get sent.…
- [ ] `li_outreach_preview` — Preview the actual copy a sequence step would generate for a prospect, WITHOUT sending.…
- [ ] `li_outreach_schedule` — The send schedule: what is projected to go out next for each active lead (simulating the throttle — the same ticks + caps the cron uses) and what actually already went out (from th…
- [ ] `li_outreach_tick` — Run one outreach tick NOW (the same pass the hourly :45 cron runs): pick due prospects, respect the throttle caps + global pause, send with jitter, advance sequences.…
- [ ] `li_outreach_funnel` — The outreach FUNNEL: connection requests → connections (accepted) → messages sent → replies, with per-stage prospect names and counts.…
- [ ] `li_outreach_send_now` — Send a scheduled LI Outreach prospect's NEXT due step RIGHT NOW — pushes their pending connect/message out immediately instead of waiting for the throttled hourly tick.…
- [ ] `li_outreach_mark_reply` — Mark that a prospect REPLIED to the outreach (or clear the mark with replied=false).…

### LinkedIn operator tools (cut; the linkedin GATEWAY stays) — `linkedin.js` (11)

- [x] `linkedin_status` — Probe the LinkedIn gateway.…
- [x] `read_linkedin_profile` — Read a LinkedIn profile by public_id (e.g.…
- [x] `me_linkedin` — Read the operator's own LinkedIn profile.…
- [x] `get_linkedin_feed` — Pull recent posts from the operator's home feed.…
- [x] `list_linkedin_dms` — List DM conversation threads, newest first.…
- [x] `read_linkedin_dm` — Read the messages of one DM thread.…
- [x] `send_linkedin_dm` — Send a direct message to a LinkedIn profile by their urn id.…
- [x] `post_linkedin_text` — FALLBACK ONLY — do NOT use this to publish.…
- [x] `react_linkedin_post` — React to a LinkedIn post by URL.…
- [x] `search_linkedin_people` — Search LinkedIn people by keywords (name, headline, company, role).…
- [x] `send_linkedin_connection` — Send a connection request.…

### Pipeline (cut) — `pipeline.js` (2)

- [ ] `list_pipeline` — List the sales pipeline — clients/deals that have a stage, plus all active/prospect/partner accounts.…
- [ ] `update_deal` — Move a deal on the pipeline board or set its value.…

### Tasks (cut) — `tasks.js` (3)

- [ ] `list_tasks` — List the daily tasks for a day (default today).…
- [ ] `update_task` — Update a daily task: set status (pending|doing|done|drafted|skipped), title, or detail.…
- [ ] `add_task` — Add a task to a day's list.…

### Website (cut) — `website-content-blocks.js` (3)

- [ ] `list_content` — List every editable copy block on the public website.…
- [ ] `read_content` — Read one website content block by slug.…
- [ ] `write_content` — Update an editable content block on the public website.…

## Gateways (14)

- [x] `llm` — Anthropic Messages API + local fallback — the only LLM boundary. REQUIRED.
- [x] `web` — generic bounded fetch of public pages/APIs (imports, feeds, IndexNow)
- [x] `assets` — Cloudflare R2 binary store (covers, figures, photos)
- [x] `image` — image generation (OpenAI Images / Workers AI) for the featured-image fallback
- [x] `social` — Make.com webhooks: LinkedIn company/personal + Facebook company posting
- [x] `whatsapp` — wa-gateway daemon: WhatsApp session, chats, sends
- [x] `linkedin` — linkedin-gateway daemon: operator LinkedIn reads/writes (Prospecting company enrich)
- [x] `pdl` — People Data Labs person enrich (skips without key)
- [x] `twilio` — Twilio Lookup: phone line type/carrier (skips without key)
- [x] `serp` — SerpApi Google Search + Lens (skips without key)
- [x] `theorg` — theorg.com org charts, no key needed
- [x] `hf` — Hugging Face fallback writer while the Anthropic breaker is open
- [x] `tts` — local Piper TTS for Nyo voice mode
- [ ] `deploy` — local static-site rebuild sidecar, localhost only

## Workflows (13)

- [x] `social-drafts-for-article` — RUNNABLE: one social draft per channel on blog publish
- [x] `outreach-replies-to-pipeline` — RUNNABLE: repliers land on the sales board hourly
- [x] `hottake-produce` — RUNNABLE: take → brief → article → review → social in one chain
- [x] `blog-shape` — observability: the composeAndSavePost pipeline
- [x] `blog-expand` — observability: lengthen an article + FAQ
- [x] `article-figures` — observability: figure specs → SVG→PNG → embed
- [x] `blog-featured-image` — observability: candidates → vision judge → cover
- [x] `social-card` — observability: share-card render
- [x] `nyo-wake-up` — observability: app-open survey → briefing
- [x] `hourly-awareness-sweep` — observability: OSINT scrape → heartbeat scoring → digest regen
- [x] `meeting-reminders` — observability: WhatsApp reminders before meetings
- [x] `aeo-suggestion-generator` — observability: OSINT signals → suggested article angles
- [ ] `li-outreach-tick` — LI outreach hourly sender (module is cut)
