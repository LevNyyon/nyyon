# WhatsApp pitches

EXTRA canned openers for the Digest card's WhatsApp composer. The basic
pitch is NOT here: it is sourced live from the gtm pack's
plugin-gtm-outreach-first-touch doc (everything after its `---` line), the
one canonical first-touch message. Placeholders: {first_name} / {name},
{company}, {role}.

```json
{ "pitches": [] }
```
